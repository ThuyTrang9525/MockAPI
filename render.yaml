services:
  - type: web
    name: my-php-app
    runtime: docker
    plan: free
    repo: https://github.com/your-repo
    region: oregon
    rootDir: /
    envVars:
      - key: APP_ENV
        value: production
    dockerCommand: docker build -t my-php-app .
